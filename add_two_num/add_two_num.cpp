#include <iostream>

using namespace std;

int main()
{
    int x, y, sum;
    cout << "Enter two integers: ";
    cin >> x >> y;
    sum = x + y;
    cout << x << " + " << y << " = " << sum << endl;
    return 0;
}